# Dependencies
node_modules/

# Build
dist/
build/
*.tsbuildinfo

# Environment (committed server/.env only defines local SQLite URL for this template)
.env
.env.local
.env.*.local
!server/.env

# Database (SQLite — keep schema/migrations in git)
server/prisma/*.db
server/prisma/*.db-journal

# Logs
logs/
*.log
npm-debug.log*

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Vite
client/dist/
